import { ONESIGNAL_APP_ID, STRIPE_PUBLISHABLE_KEY, API_URL } from '@env';

export const oneSignalAppId = ONESIGNAL_APP_ID;
export const stripePublishableKey = STRIPE_PUBLISHABLE_KEY;
export const apiUrl = API_URL;